node_modules
dist
build
.env
.env.*
# .env.* above is meant to keep real secrets (.env, .env.local,
# .env.production) out of git, not the checked-in template. Un-ignore it
# explicitly — same class of landmine as the old `*data*` glob eating
# MetadataList.jsx (DEPLOY.md, DOCS/RUNBOOK.md "known failure modes").
!.env.example
.DS_Store
data/
.claude/
.sixth/
dev-dist

# macOS AppleDouble metadata (SMB access)
._*

# Personal health-scan source files — see DOCS/BODY_COMPOSITION_INTAKE.md.
# Numbers live in test fixtures; the raw reports carry name/ID and stay untracked.
DOCS/screenshots/arboleaf.*

# Chef's real Playnite export — personal data, never commit (public repo)
DOCS/playnite-library.json

# BookGeek tag review worksheet: built from Chef's real tags, kept local (regenerate: node apps/bookgeek/api/scripts/tags-review.mjs)
apps/bookgeek/DOCS/TAGS_REVIEW.md
