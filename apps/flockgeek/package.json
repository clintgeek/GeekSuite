{
  "name": "flockgeek",
  "version": "0.1.0",
  "private": true,
  "description": "Root orchestrator for FlockGeek frontend and backend services",
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev": "npm run start",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
