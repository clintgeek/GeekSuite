// User model removed: this template intentionally avoids including a database
// user model so the repository remains generic and aligned with the
// enhancement document. Replace with your own persistence model when
// integrating a database.

export const User = null;
